// import React from 'react';
// import { API_URL, API_ENDPOINTS } from '../constants/constants';

const makeRequest = (type, endpoint, payload) => {
    
}

const makeExternalRequest = (type, endpoint, payload) => {

}

export {
    makeRequest,
    makeExternalRequest,
}